import os
import logging
import asyncio
import threading
import time
from telegram import Update
from telegram.constants import ParseMode
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
from rapidfuzz.fuzz import token_set_ratio 
from flask import Flask

# --- CONFIGURATION ---
TOKEN = os.environ.get("BOT_TOKEN", "")

# এডমিন আইডি হ্যান্ডলিং
admin_ids_str = os.environ.get("ADMIN_IDS", "7870088579,7259050773")
try:
    ADMIN_IDS = [int(x.strip()) for x in admin_ids_str.split(",") if x.strip().isdigit()]
except:
    ADMIN_IDS = [7870088579, 7259050773] 

GROUP_CHAT_ID = os.environ.get("GROUP_CHAT_ID", "-1002337825231")

# --- FLASK SERVER ---
app = Flask(__name__)

@app.route('/')
def home():
    return "Skyzone IT Bot is Online!"

def run_flask():
    port = int(os.environ.get("PORT", 10000))
    app.run(host='0.0.0.0', port=port, use_reloader=False)

# --- LOGGING ---
logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO)
logger = logging.getLogger(__name__)

# --- CONFIGURATION DATA ---
bot_config = {
    "video_link": "https://www.youtube.com/", 
    "video_text": "আমাদের গ্রুপে নতুন তাই ভিডিওটি সম্পূর্ণ দেখুন। ভিডিওটি দেখার শেষ হলে, এই বটটিতে গিয়ে 'IT' লিখে সকল প্রশ্নের উত্তর দিবেন।",
    "terms_text": """
⚠️ **আপনাকে এই শর্তগুলো দেওয়া হল, মেনে চলতে হবে** ⚠️

1️⃣ সাবধান: যে অ্যাপের জন্য টেক্সট তৈরি করা হবে, সেই অ্যাপেই রিভিউ দিতে হবে।

2️⃣ একবার সাবমিট: আপনি যে অ্যাপে কাজ সাবমিট করবেন একবার করে ফেললে দ্বিতীয়বার আর করবেন না।

3️⃣ সময় মেনে চলা: অ্যাপস যে সময় দেওয়া থাকবে, সেই সময় থেকেই কাজ শুরু করবেন।

4️⃣ একটি ফোন, একটি জিমেইল: একটি ফোন ও একটি জিমেইল দিয়ে একবার রিভিউ দিবেন।

5️⃣ নতুন মানুষ আনা: আপনি যেভাবে এখানে এসেছেন, ঠিক সেইভাবেই অন্যদেরও নিয়ে আসবেন।

6️⃣ সঠিক গ্রুপ এড: আপনার থেকে বেশি বোঝে এমন কাউকে গ্রুপে এড করবেন না।

7️⃣ পেমেন্ট স্ক্রিনশট: পেমেন্ট পাওয়ার পর পেমেন্টের স্ক্রিনশট গ্রুপে পোস্ট করতে হবে।

8️⃣ ভদ্র আচরণ: সবার সাথে ভালো ব্যবহার করবেন।

9️⃣ ২৪ ঘণ্টা নিয়ম: আপনি যাদের দিয়ে রিভিউ করাবেন, তাদেরকে ২৪ ঘণ্টা পর গ্রুপে এড করতে হবে।

🔟 সমস্যা সমাধান: কোনো সমস্যা হলে ভিডিও দেখে সমাধান করবেন।

**সতর্কবার্তা:**
❌ একই লোকেশন বা ফ্যামিলি ফোন থেকে রিভিউ দেওয়া যাবে না।
❌ নির্ধারিত সময়ের আগে মার্কেটিং করা যাবে না।

**ফলাফল:**
🚫 অ্যাকাউন্ট ব্যান ও ব্যালেন্স ফ্রিজ হবে।

**সকল শর্ত মেনে চললে আমাকে রিপ্লাই দিন:** "**ইনশাআল্লাহ আমি পারবো**"
— SKYZONE IT Admin™
""",
    "final_phrase": "ইনশাআল্লাহ আমি পারবো",
    "form_link": "https://forms.gle/TYdZFiFEJcrDcD2r5", 
    "admin_username": "@AfMdshakil" 
}

# --- QUESTIONS DB (আপনার প্রথম কোড থেকে হুবহু উত্তরগুলো নেওয়া হয়েছে) ---
questions_db = [
    {"id": 1, "q": "1️⃣ আপনি কি ভিডিওটি সম্পূর্ণ মনোযোগ দিয়ে দেখেছেন?", "a": ["hea", "ji", "yes", "ha", "সম্পূর্ণ ভিডিও দেখছি", "জি", "ho", "dekhsi"], "threshold": 70},
    {"id": 2, "q": "2️⃣ ভিডিও দেখে আপনি কী বুঝেছেন?", "a": ["Kivabe app use Korte hobe", "ভিডিওটি দেখে বুঝতে পারছি আমি যেভাবে এখানে আইসি সেভাবেই অন্যদেরকে নিয়ে আসতে হবে", "পরবর্তী", "ভিডিও দেখে সকল কিছু শিখতে পারলাম", "Facebook e post kore user k telegram e aina", "review apnder app e submit dite hobe", "marketing korbo", "apps review"], "threshold": 50},
    {"id": 3, "q": "3️⃣ আপনি কোন ফোন থেকে রিভিউ দেবেন? (নিজের/পরিবারের ফোন ও একই লোকেশন নিষিদ্ধ)", "a": ["ami nijer phn theke review dibo na", "অন্যদের ফোন থেকে", "মার্কেটিং করে অন্যদের ফোন থেকে রিভিউ দেওয়াতে হবে", "review amr worker dibe", "worker er phone", "onno manush diye", "user er phone"], "threshold": 60},
    {"id": 4, "q": "4️⃣ আপনি মোট কয়টি রিভিউ দিতে পারবেন?", "a": ["joto golo limit thakbe", "5 tar moto", "ফেসবুক এবং টেলিগ্রামের বিভিন্ন গ্রুপ থেকে মেম্বার খুজে তাদের ফোন থেকে রিভিউ দেওয়াবো, এবং তাদের রিভিৎ এপ্রুভ হলে তাদেরকে গ্রুপে এড করে দিবো।", "অ্যাপে যে লিমিট দেওয়া থাকবে ওই অনুযায়ী দিতে পারব", "অ্যাপের নির্দেশনা অনুযায়ী দিতে পারব", "unlimited", "jotogula lagbe"], "threshold": 50},
    {"id": 5, "q": "5️⃣ আপনার কি আগে থেকে কোনো অভিজ্ঞতা আছে, নাকি একদম নতুন?", "a": ["noton", "new", "অভিজ্ঞতা আছে", "আমি একদম নতুন", "নতুন", "অভিজ্ঞতা আছে", "experience nai", "agerr oviggota ace"], "threshold": 60},
    {"id": 6, "q": "6️⃣ আপনি দিনে কোন সময়ে কাজ করতে স্বাচ্ছন্দ্যবোধ করবেন?", "a": ["user jeita bolbe", "নির্দিষ্ট সময় নাই", "অ্যাপে যে সময় দেওয়া থাকবে ওই সময় থেকে", "আপনারা যে সময় দিবেন ওই সময় থেকে", "jekono somoy", "shokal", "bikal", "rat", "all time"], "threshold": 40},
    {"id": 7, "q": "7️⃣ আপনি কি এই কাজগুলোর দায়িত্ব নিয়ে নিয়মিত করতে পারবেন?", "a": ["hea", "ji", "yes", "ইনশাআল্লাহ পারবো", "চেষ্টা করব", "ইনশাআল্লাহ", "অবশ্যই", "জি", "parbo"], "threshold": 80},
    {"id": 8, "q": "8️⃣ আমাদের সব নিয়ম ও শর্ত মেনে কাজ করতে পারবেন তো?", "a": ["hea", "ji", "yes", "parbo", "ইনশাআল্লাহ", "সব শর্ত মানব", "চেষ্টা করব", "ইনশাআল্লাহ চেষ্টা করব", "InshaAllah"], "threshold": 80},
    {"id": 9, "q": "9️⃣ ভিডিওতে বলা হয়েছে — সর্বনিম্ন কত টাকা হলে উত্তোলন করা যাবে?", "a": ["50", "panchas", "৫০", "৫০ টাকা", "সর্বনিম্ন ৫০ টাকা", "ponchash"], "threshold": 90},
    {"id": 10, "q": "🔟 আপনি কীভাবে মার্কেটিং করতে চান? (সংক্ষেপে)", "a": ["Facebook e post kore", "ফেসবুক মার্কেটিং করে", "ফেসবুক মার্কেটিং করে বিভিন্ন গ্রুপে পোস্ট করে", "userder sathe contect kore", "social media", "marketing kore"], "threshold": 50}
]

USER_DATA = {}
S_IDLE, S_READY_CHECK, S_INTERVIEW, S_WAITING_PHRASE, S_FORM_FILLED = range(5)

# --- HELPERS ---
def check_answer_ai(user_text, expected_answers, threshold):
    best_score = 0
    if not user_text: return False
    for ans in expected_answers:
        score = token_set_ratio(user_text.lower(), ans.lower())
        if score > best_score: best_score = score
    return best_score >= threshold

# --- HANDLERS ---

async def handle_new_member(update: Update, context: ContextTypes.DEFAULT_TYPE):
    for member in update.message.new_chat_members:
        if member.id == context.bot.id: continue
        welcome = (
            f"স্বাগতম {member.mention_html()}! 🎉\n\n"
            f"{bot_config['video_text']}\n"
            f"👉 <a href='{bot_config['video_link']}'>ভিডিওটি দেখতে এখানে ক্লিক করুন</a>\n\n"
            f"ভিডিও দেখা শেষ হলে কাজ শুরু করতে আমাকে ইনবক্সে <b>'IT'</b> লিখে মেসেজ দিন।"
        )
        await update.message.reply_text(welcome, parse_mode=ParseMode.HTML, disable_web_page_preview=True)

async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user = update.effective_user
    msg = update.message.text.strip() if update.message.text else ""
    chat_type = update.effective_chat.type
    uid = user.id

    if not msg: return

    # গ্রুপ মেসেজ লজিক (পুরাতন মেম্বারদের বিরক্ত করবে না, শুধু নতুন মেম্বারদের কিওয়ার্ডে উত্তর দিবে)
    if chat_type != 'private':
        triggers = ["it", "আমি নতুন মেম্বার", "ami new member", "আমি কাজ করতে চাই", "কাজ কি", "কি কাজ"]
        if any(t.lower() in msg.lower() for t in triggers):
            await update.message.reply_text(
                f"{user.mention_html()}, কাজের জন্য আমার ইনবক্সে 'IT' লিখুন। ভিডিওটি উপরে পিন করা আছে, সেটি দেখে নিন।",
                parse_mode=ParseMode.HTML
            )
        return

    # ইনবক্স ইন্টারভিউ লজিক
    if uid not in USER_DATA: USER_DATA[uid] = {"state": S_IDLE, "answers": [], "q_idx": 0}
    state = USER_DATA[uid]["state"]

    if msg.upper() == 'IT':
        USER_DATA[uid] = {"state": S_READY_CHECK, "answers": [], "q_idx": 0}
        await update.message.reply_text("আপনি কি ১০টি প্রশ্নের উত্তর দিতে প্রস্তুত?\n(উত্তর দিন: Yes / Ready / প্রস্তুত)")
        return

    if state == S_READY_CHECK:
        if any(word in msg.lower() for word in ['yes', 'ready', 'ha', 'hea', 'ji', 'prostut', 'start']):
            USER_DATA[uid]["state"] = S_INTERVIEW
            await update.message.reply_text(f"চমৎকার! শুরু করছি।\n\n{questions_db[0]['q']}")
        else:
            await update.message.reply_text("প্রস্তুত থাকলে 'Yes' বা 'Ready' লিখুন।")
        return

    if state == S_INTERVIEW:
        idx = USER_DATA[uid]["q_idx"]
        current_q = questions_db[idx]
        if check_answer_ai(msg, current_q['a'], current_q['threshold']):
            USER_DATA[uid]["answers"].append({"q": current_q['q'], "a": msg})
            if idx + 1 < len(questions_db):
                USER_DATA[uid]["q_idx"] += 1
                await update.message.reply_text(f"✅ সঠিক উত্তর!\n\n{questions_db[idx+1]['q']}")
            else:
                USER_DATA[uid]["state"] = S_WAITING_PHRASE
                await update.message.reply_text(f"অভিনন্দন! ১০টি প্রশ্নের সঠিক উত্তর দিয়েছেন। 🎉\n{bot_config['terms_text']}")
        else:
            await update.message.reply_text("❌ আপনার উত্তর সঠিক হয়নি। আবার চেষ্টা করুন।")
        return

    if state == S_WAITING_PHRASE:
        if token_set_ratio(msg.lower(), bot_config['final_phrase'].lower()) > 85:
            USER_DATA[uid]["state"] = S_FORM_FILLED
            notice = f"⚡ **Official Notice – SKYZONE IT** ⚡\n✅ উত্তর ও শর্ত সঠিক হয়েছে।\n📋 এখন নিচের ফর্মটি পূরণ করুন:\n🔗 <a href='{bot_config['form_link']}'>Form Link👈</a>\n\nপূরণ শেষে এখানে লিখুন: <b>Form Done</b>"
            await update.message.reply_text(notice, parse_mode=ParseMode.HTML, disable_web_page_preview=True)
        else:
            await update.message.reply_text(f"বাক্যটি সঠিক হয়নি। হুবহু লিখুন:\n`{bot_config['final_phrase']}`", parse_mode=ParseMode.MARKDOWN)
        return

    if state == S_FORM_FILLED:
        if any(word in msg.lower() for word in ['form done', 'slip', 'done']):
            report = f"📄 **SKYZONE IT - RECRUITMENT SLIP**\n👤 User: {user.mention_html()}\n🆔 ID: <code>{uid}</code>\n✅ Status: Passed"
            
            # এডমিনদের কাছে অটো রিপোর্ট পাঠানো
            for aid in ADMIN_IDS:
                try: await context.bot.send_message(chat_id=aid, text=f"📩 **নতুন মেম্বার পাস করেছে!**\n\n{report}", parse_mode=ParseMode.HTML)
                except: pass

            # ইউজারকে স্লিপ দেওয়া
            await update.message.reply_text(report, parse_mode=ParseMode.HTML)
            await update.message.reply_text(f"✅ আপনার স্লিপটি কপি করে এডমিনকে পাঠান।\n👉 এডমিন: {bot_config['admin_username']}", parse_mode=ParseMode.HTML)
            USER_DATA[uid]["state"] = S_IDLE
        return

# --- MAIN ---
def main():
    threading.Thread(target=run_flask, daemon=True).start()
    application = Application.builder().token(TOKEN).build()
    
    application.add_handler(MessageHandler(filters.StatusUpdate.NEW_CHAT_MEMBERS, handle_new_member))
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
    
    application.run_polling(drop_pending_updates=True)

if __name__ == "__main__":
    main()
